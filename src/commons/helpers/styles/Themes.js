import { createTheme } from "@mui/material";

const globalTheme = createTheme({
  palette: {
    primary: {
      main: "#f9a825",
    },
    secondary: {
      main: "#ff9800",
    },
  },
});

export default globalTheme;
