import { createTheme } from "@mui/material";

export const globalTheme = createTheme({
    palette: {
        primary: {
            main: '#00bcd4',
        },
        secondary: {
            main: '#ff9800',
        }
    }
});
